# Label Link Type Extension

Adds the 'Label' type for menu links.

Also comes with a custom JS menu link type which is automatically registered as a asynchronous component and thus only loaded when used.
